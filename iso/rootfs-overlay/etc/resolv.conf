# Copper Linux - handcrafted by 12hrformat
#
# Fallback resolver. udhcpc's default.script rewrites this file the moment a
# lease arrives, so this only matters on a network that hands out addresses
# but no nameservers, or before DHCP has said anything at all.
nameserver 1.1.1.1
nameserver 9.9.9.9
